{
  "version": "2.0.0",
  "tasks": [
    { "label": "Tests (pytest)", "type": "shell", "command": ". .venv/bin/activate && PYTHONPATH=. python -m pytest -q" },
    { "label": "Lint (ruff) + Fix", "type": "shell", "command": ". .venv/bin/activate && ruff format . && ruff check --fix ." },
    { "label": "Type check (mypy)", "type": "shell", "command": ". .venv/bin/activate && python -m mypy ." },
    { "label": "Security (pip-audit)", "type": "shell", "command": ". .venv/bin/activate && pip-audit" }
  ]
}
